<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.device.camera</key>
    <true/>
    <key>com.apple.security.automation.apple-events</key>
    <true/>
    <!-- Audit-2 fix: required for pynput keyboard/mouse capture under
         a real Developer-ID signed + hardened-runtime build. The
         daemon's NSInputMonitoringUsageDescription is declared in
         cortex.spec; without this entitlement, signed builds silently
         drop typing-burst / mouse-thrashing telemetry. Ad-hoc builds
         (no hardened runtime) ignore it. -->
    <key>com.apple.security.device.input-monitoring</key>
    <true/>
</dict>
</plist>
